{
  "packages": ["packages/*"],
  "version": "independent",
  "useWorkspaces": true,
  "npmClient": "yarn",
  "changelogPreset": "conventionalcommits",
  "issueUrlFormat": "{{host}}/{{owner}}/{{repository}}/issues/{{id}}",
  "conventionalCommits": true,
  "command": {
    "version": {
      "allowBranch": ["main", "initial-setup"]
    }
  }
}
